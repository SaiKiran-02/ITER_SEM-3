import math

print("""
NOTE:
UNITS OF THE PHYSICAL QUANTITIES MUST BE  S.I UNIT""")
length = float(input("Enter the length of room: "))
width = float(input("Enter the width of the room:"))

area = length * width

print(f"Area of the room having length {length} and width {width} is {area}")
